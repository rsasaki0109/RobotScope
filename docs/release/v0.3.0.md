# RobotScope v0.3.0

Third **general availability** release — rosbag2 sidecar cache on v0.2 foundations.

## What v0.3.0 means

- **Plugin API `0.1`** remains stable (unchanged from v0.1/v0.2 GA)
- **No breaking changes** from `0.3.0-beta.0` — GA tag + documentation
- Future **`0.3.x`** patches: bugfixes only; new subsystems land in **v0.4+**

## Shipped in v0.3

| Area | Capability |
|------|------------|
| **Rosbag2 sidecar** | IndexedDB cache with `recording_source: rosbag2`; re-open `.db3` skips topic timestamp re-scan |
| **Sidecar format** | `[time_ns, storage_id]` tuples (distinct from MCAP `[time_ns, sequence?]`) |
| **Fast path** | `loadRosbag2Payloads()` + `indexRosbag2TfOnly()` — payloads + TF still loaded from sqlite |
| **v0.2 carry-over** | Cross-layout recipe banner, lazy sql.js rosbag2 ingest, MCAP, live agent, plugins, Lanelet2 RL2D |

## Quick paths

| Goal | URL / command |
|------|----------------|
| Demo MCAP | https://rsasaki0109.github.io/RobotScope/?layout=autoware&demo=1 |
| Open rosbag2 | **Open recording** → `.db3` (first open builds sidecar) |
| Re-open speedup | Open the **same** `.db3` again → status shows **rosbag2 sidecar** |
| Convert fallback | `npm run convert:rosbag2 -- bag.db3 out.mcap` |

## Release history (v0.3 line)

| Series | Tags |
|--------|------|
| Alpha | `0.3.0-alpha.0` |
| Beta | `0.3.0-beta.0` |
| **GA** | **`0.3.0`** |

Full changelog: [CHANGELOG.md](RobotScope/CHANGELOG.md).

## Known limitations

[docs/known-limitations.md](RobotScope/docs/known-limitations.md) — rosbag2: single `.db3`, payloads always reloaded; Lanelet2: RL2D demo only.

## Toward v0.4

- Rosbag2 folder bags (`metadata.yaml` + multi-file)
- Native Autoware `lanelet2_io` map parsing
- Optional command gateway (permission-gated)
