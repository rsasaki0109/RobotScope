# RobotScope v1.0.0

Tenth **general availability** release — full 6-DOF Twist editor on v0.9 command gateway foundations.

## What v1.0.0 means

- **Plugin API `0.1`** remains stable (unchanged from v0.1–v0.9 GA)
- **No breaking changes** from `1.0.0-beta.0` — GA tag + documentation
- Future **`1.0.x`** patches: bugfixes only; new subsystems land in **v1.1+**

## Shipped in v1.0

| Area | Capability |
|------|------------|
| **6-DOF Twist editor** | Command bar **vx / vy / vz / ωx / ωy / ωz** + **Publish cmd_vel** + **Zero cmd_vel** |
| **Command gateway** | v0.9 carry-over — agent allowlist + viewer **Allow publish** opt-in |
| **Live protocol** | Full `twist` payload on `command.publish` + `command.publish_result` |
| **Read-only playback** | MCAP / rosbag2 / GitHub Pages demo unchanged |
| **v0.9 carry-over** | Command gateway, OSM maps, rosbag2 folder bags, failure recipes, plugins |

## Quick paths

| Goal | URL / command |
|------|----------------|
| Demo MCAP | https://rsasaki0109.github.io/RobotScope/?layout=autoware&demo=1 |
| Demo live + twist | `npm run demo:live-agent` → Connect Live → Allow publish → Publish cmd_vel |
| Native ROS | `npm run demo:ros2-agent -- --profile nav2 --allow-publish /cmd_vel` |
| Folder bag | **Open bag folder** → `metadata.yaml` + `.db3` |

## Release history (v1.0 line)

| Series | Tags |
|--------|------|
| Alpha | `1.0.0-alpha.0` |
| Beta | `1.0.0-beta.0` |
| **GA** | **`1.0.0`** |

Full changelog: [CHANGELOG.md](RobotScope/CHANGELOG.md).

## Known limitations

[docs/known-limitations.md](RobotScope/docs/known-limitations.md) — No service/action gateway; plugin manifest permissions not enforced at runtime.

## Toward v1.1+

- Native Autoware `lanelet2_io` boost binary parsing
- Service / action gateway (future)
